# =============================================================================
# bookstore-frontend — React/Nginx | Port 80
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstore-frontend
  namespace: ns-bookstoreapp
  labels:
    app: bookstore-frontend
    app.kubernetes.io/part-of: bookstoreapp
    version: "1.0.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bookstore-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: bookstore-frontend
        version: "1.0.0"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        fsGroup: 101
      containers:
        - name: bookstore-frontend
          image: mokadir/bookstore-frontend:1
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: REACT_APP_API_URL
              value: "/api"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "128Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: bookstore-frontend
  namespace: ns-bookstoreapp
  labels:
    app: bookstore-frontend
spec:
  selector:
    app: bookstore-frontend
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30022
      protocol: TCP
